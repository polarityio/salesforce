let assert = require('chai').assert;
let integration = require('./integration');

describe('salesforce polarity integration', () => {
    it('should display matching leads', () => {

    });

    it('should display matching contacts', () => {

    });

    it('should display matching opportunities', () => {

    });
});
